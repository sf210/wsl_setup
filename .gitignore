# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/
dist/
build/

# Node
node_modules/
.next/
coverage/

# Environment & secrets
.env
.env.*
!.env.example
*.pem
*.key

# Data — keep the layout, not the payload
data/raw/*
data/interim/*
data/processed/*
!data/**/.gitkeep

# Editors / OS
.idea/
.vscode/
.DS_Store

# Claude Code local state
.claude/
.claude/settings.local.json

# Sandbox: symlinks to host paths are machine-specific, as are local overrides.
# .sandbox/binds.conf IS tracked — it documents what this project needs.
/external/
.sandbox/binds.local.conf
